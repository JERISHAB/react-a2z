import React from 'react'

const Create = (props) => {

    
  return (
    //   React.createElement('div',{className: 'title'},'Hellow world')
    <h1>{this.props.name}</h1>
  );
};

export default Create